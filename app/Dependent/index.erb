<div class="pageTitle">
  <h1>Dependents</h1>
</div>

<div class="toolbar">
  <div class="leftItem regularButton">
    <a href="<%= Rho::RhoConfig.start_path %>">Home</a>
  </div>
  <div class="rightItem regularButton">
    <a class="button" href="<%= url_for :action => :new %>">New</a>
  </div>
</div>

<div class="content">
  <ul>
    <% @dependents.each do |dependent| %>
    
        <li>
          <a href="<%= url_for :action => :show, :id => dependent.object %>">
            <span class="title"><%= dependent.name %></span><span class="disclosure_indicator"></span>
          </a>
        </li>
    
    <% end %>
  </ul>
</div>