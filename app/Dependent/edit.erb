<div class="pageTitle">
  <h1>Edit <%= @dependent.name %></h1>
</div>

<div class="toolbar">
  <div class="leftItem backButton">
    <a href="<%= url_for :action => :show, :id => @dependent.object %>">Cancel</a>
  </div>
  <div class="rightItem regularButton">
    <a class="button" href="<%= url_for :action => :delete, :id => @dependent.object %>">Delete</a>
  </div>
</div>

<div class="content">
  <form method="POST" action="<%= url_for :action => :update %>">
    <input type="hidden" name="id" value="<%= @dependent.object %>"/>
    <ul>
      
          <li>
            <label for="dependent[name]" class="fieldLabel">Name</label>
            <input type="text" name="dependent[name]" value="<%= @dependent.name %>" <%= placeholder( "Name" ) %> />
          </li>
      
    </ul>
    <input type="submit" class="standardButton" value="Update"/>
  </form>
</div>